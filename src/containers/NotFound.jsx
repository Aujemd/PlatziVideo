import React from 'react'

//El fragment ahora un nodo en el arbol de nodos del dom para cuando se quiere enviar varios elementos
const NotFound = () => (
    <React.Fragment> 
        <h1>No Encontrado</h1>
    </React.Fragment>
);

export default NotFound;